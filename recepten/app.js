const recipes=window.HUIZE_CHAOS_RECIPES||[];const list=document.querySelector('#list'),detail=document.querySelector('#detail'),search=document.querySelector('#search');let current=null;let edited=null;function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function renderList(){const q=search.value.trim().toLowerCase();const a=recipes.filter(r=>r.title.toLowerCase().includes(q)).slice(0,100);list.innerHTML=a.length?a.map((r,i)=>`<button class="recipe-card" data-i="${recipes.indexOf(r)}"><span><strong>${esc(r.title)}</strong><small>${r.servings?esc(r.servings)+' personen':''}</small></span><span class="go">›</span></button>`).join(''):`<div class="empty">Geen recepten gevonden.</div>`;list.querySelectorAll('.recipe-card').forEach(b=>b.onclick=()=>openRecipe(+b.dataset.i))}function openRecipe(i){current=i;edited=JSON.parse(JSON.stringify(recipes[i]));list.classList.add('hidden');search.classList.add('hidden');detail.classList.remove('hidden');showView('ingredients')}function showView(view){const r=edited;detail.innerHTML=`<div class="detail-head"><div><h2>${esc(r.title)}</h2><small>${r.servings?esc(r.servings)+' personen':''}</small></div><div class="actions"><button class="btn" id="backList">Terug</button></div></div><div class="tabs"><button class="tab ${view==='ingredients'?'active':''}" data-v="ingredients">Ingrediënten</button><button class="tab ${view==='directions'?'active':''}" data-v="directions">Bereiding</button><button class="tab ${view==='edit'?'active':''}" data-v="edit">Wijzigen</button></div>${view==='ingredients'?ingredients(r):view==='directions'?`<div class="panel directions">${esc(r.directions)}</div>`:edit(r)}`;detail.querySelector('#backList').onclick=()=>{detail.classList.add('hidden');list.classList.remove('hidden');search.classList.remove('hidden');};detail.querySelectorAll('.tab').forEach(b=>b.onclick=()=>showView(b.dataset.v));if(view==='edit')bindEdit()}function ingredients(r){return `<div class="panel">${r.ingredients.map(x=>`<div class="ing"><span class="qty">${esc(x.qty)}</span><span class="unit">${esc(x.unit)}</span><span>${esc(x.ingredient)}</span>${x.memo?`<span class="memo">${esc(x.memo)}</span>`:''}</div>`).join('')}</div>`}function edit(r){return `<div class="edit-section" data-sec="ingredients"><button>Ingrediënten <span>▸</span></button><div class="edit-body">${r.ingredients.map((x,i)=>`<div class="edit-row"><input data-f="qty" data-i="${i}" value="${esc(x.qty)}"><input data-f="unit" data-i="${i}" value="${esc(x.unit)}"><input data-f="ingredient" data-i="${i}" value="${esc(x.ingredient)}"></div><div class="edit-row"><span></span><span></span><input data-f="memo" data-i="${i}" value="${esc(x.memo)}" placeholder="Memo"></div>`).join('')}</div></div><div class="edit-section" data-sec="directions"><button>Bereiding <span>▸</span></button><div class="edit-body"><textarea id="directionsEdit">${esc(r.directions)}</textarea></div></div><div class="actions"><button class="btn primary" id="save">Opslaan</button><button class="btn" id="cancel">Annuleren</button></div>`}function bindEdit(){detail.querySelectorAll('.edit-section>button').forEach(b=>b.onclick=()=>b.parentElement.classList.toggle('open'));detail.querySelectorAll('input[data-f]').forEach(el=>el.oninput=()=>edited.ingredients[+el.dataset.i][el.dataset.f]=el.value);detail.querySelector('#directionsEdit').oninput=e=>edited.directions=e.target.value;detail.querySelector('#save').onclick=()=>{localStorage.setItem('hc_recipe_'+edited.id,JSON.stringify(edited));recipes[current]=edited;showView('ingredients')};detail.querySelector('#cancel').onclick=()=>{edited=JSON.parse(JSON.stringify(recipes[current]));showView('ingredients')}}search.oninput=renderList;renderList();