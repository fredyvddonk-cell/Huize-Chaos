const KEY='huize-chaos-occasions-v1';let activeEventId='';let events=JSON.parse(localStorage.getItem(KEY)||'[]');const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const money=n=>new Intl.NumberFormat('nl-NL',{style:'currency',currency:'EUR'}).format(Number(n)||0);function save(){localStorage.setItem(KEY,JSON.stringify(events));render()}function catTotals(e){return ['Eten','Hapjes','Dranken','Overig'].map(c=>[c,(e.items||[]).filter(x=>x.category===c).reduce((a,x)=>a+(+x.cost||0),0)])}function render(){const list=$('#list');if(!events.length){list.innerHTML='<div class="empty"><h2>Nog geen gelegenheden</h2><p>Voeg een feestdag, verjaardag of andere gelegenheid toe.</p></div>';return}list.innerHTML=[...events].sort((a,b)=>(b.date||'').localeCompare(a.date||'')).map(e=>{const total=(e.items||[]).reduce((a,x)=>a+(+x.cost||0),0);return `<article class="event"><div class="event-head"><div><h2>${esc(e.name)}</h2><div class="meta">${esc(e.date||'Geen datum')} · ${e.people?esc(e.people)+' personen':'Aantal personen niet ingevuld'}</div></div><button onclick="openDetail('${e.id}')">Bekijken</button></div><div class="totals">${catTotals(e).filter(x=>x[1]).map(([c,n])=>`<span class="chip">${c}: <strong>${money(n)}</strong></span>`).join('')}<span class="chip">Totaal: <strong>${money(total)}</strong></span></div></article>`}).join('')}function openForm(e={}){$('#formTitle').textContent=e.id?'Gelegenheid wijzigen':'Nieuwe gelegenheid';$('#eventId').value=e.id||'';$('#eventName').value=e.name||'';$('#eventDate').value=e.date||'';$('#eventPeople').value=e.people||'';$('#eventNote').value=e.note||'';$('#eventDialog').showModal()}$('#addEvent').onclick=()=>openForm();$('#cancelEvent').onclick=()=>$('#eventDialog').close();$('#eventForm').onsubmit=ev=>{ev.preventDefault();const id=$('#eventId').value||String(Date.now());let e=events.find(x=>x.id===id);if(!e){e={id,items:[],evaluation:''};events.push(e)}e.name=$('#eventName').value.trim();e.date=$('#eventDate').value;e.people=$('#eventPeople').value;e.note=$('#eventNote').value.trim();save();$('#eventDialog').close();openDetail(id)};window.openDetail=id=>{activeEventId=id;const e=events.find(x=>x.id===id);if(!e)return;const total=(e.items||[]).reduce((a,x)=>a+(+x.cost||0),0);const sums=catTotals(e);$('#detail').innerHTML=`<div class="detail-top"><div><small>Feestdagen & gelegenheden</small><h2>${esc(e.name)}</h2><div class="meta">${esc(e.date||'Geen datum')} · ${e.people?esc(e.people)+' personen':''}</div>${e.note?`<p>${esc(e.note)}</p>`:''}</div><button class="close" onclick="detailDialog.close()">×</button></div><div class="summary">${sums.map(([c,n])=>`<div class="sum"><small>${c}</small><strong>${money(n)}</strong></div>`).join('')}</div><h3>Gekochte producten</h3><div id="items">${(e.items||[]).map((x,i)=>itemHtml(x,i)).join('')||'<p class="meta">Nog geen producten toegevoegd.</p>'}</div><div class="add-row"><strong>Product toevoegen</strong><div class="add-grid"><input id="newProduct" placeholder="Product"><select id="newCategory"><option>Eten</option><option>Hapjes</option><option>Dranken</option><option>Overig</option></select><input id="newQty" placeholder="Aantal"><input id="newCost" inputmode="decimal" placeholder="Kosten €"><select id="newResult"><option value="">Evaluatie</option><option>Te veel</option><option>Precies goed</option><option>Te weinig</option></select></div><div class="actions"><button class="primary" onclick="addItem('${id}')">+ Toevoegen</button></div></div><div class="evaluation"><label>Algemene evaluatie<textarea id="evaluation" rows="3" placeholder="Wat wil je de volgende keer anders doen?">${esc(e.evaluation||'')}</textarea></label><button onclick="saveEvaluation('${id}')">Evaluatie opslaan</button></div><div class="actions"><button onclick="editEvent('${id}')">Gelegenheid wijzigen</button><button class="danger" onclick="deleteEvent('${id}')">Verwijderen</button><button class="primary" onclick="detailDialog.close()">Klaar</button></div>`;$('#detailDialog').showModal()};function itemHtml(x,i){return `<div class="item"><input class="product" value="${esc(x.product)}" onchange="updateItem(${i},'product',this.value)"><select onchange="updateItem(${i},'category',this.value)">${['Eten','Hapjes','Dranken','Overig'].map(c=>`<option ${x.category===c?'selected':''}>${c}</option>`).join('')}</select><input value="${esc(x.qty||'')}" placeholder="Aantal" onchange="updateItem(${i},'qty',this.value)"><input value="${esc(x.cost||'')}" inputmode="decimal" placeholder="Kosten" onchange="updateItem(${i},'cost',this.value)"><select onchange="updateItem(${i},'result',this.value)"><option value="">—</option>${['Te veel','Precies goed','Te weinig'].map(c=>`<option ${x.result===c?'selected':''}>${c}</option>`).join('')}</select><button class="del danger" onclick="deleteItem(${i})">×</button></div>`}function current(){return events.find(x=>x.id===activeEventId)}window.addItem=id=>{const e=events.find(x=>x.id===id);const product=$('#newProduct').value.trim();if(!product)return;e.items.push({product,category:$('#newCategory').value,qty:$('#newQty').value,cost:$('#newCost').value.replace(',','.'),result:$('#newResult').value});save();openDetail(id)};window.updateItem=(i,k,v)=>{const e=current();if(!e)return;e.items[i][k]=k==='cost'?v.replace(',','.'):v;save()};window.deleteItem=i=>{const e=current();if(!e)return;e.items.splice(i,1);save();openDetail(e.id)};window.saveEvaluation=id=>{const e=events.find(x=>x.id===id);e.evaluation=$('#evaluation').value;save();openDetail(id)};window.editEvent=id=>{const e=events.find(x=>x.id===id);$('#detailDialog').close();openForm(e)};window.deleteEvent=id=>{if(!confirm('Deze gelegenheid verwijderen?'))return;events=events.filter(x=>x.id!==id);save();$('#detailDialog').close()};render();
